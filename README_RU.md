<h1 align="center">
    <img src="img/logo.png" width="220" />
    <div>
    SSH Bot
    </div>
</h1>

<h4 align="center">
    <a href="README.md">English (🇺🇸)</a> | <strong>Русский (🇷🇺)</strong>
</h4>

Это Telegram бот, который позволяет запускать заданные команды на удаленных машинах и возвращать результат их выполнения без установки постоянного соединения.

Бот предоставляет возможность не тратить время на настройку `VPN` сервера и деньги на внешний ip-адрес или `VPS` сервер для доступа к локальной сети, а также избавляет от необходимости использования сторонних приложений (`VPN` и `ssh` клиентов) на удаленном устройстве и не требует стабильного Интернет соединения.

![example](/img/example.gif)

## Дорожная карта

- [X] Выполнение команд на локальном (на котором запущен бот) или удаленном хосте (через `ssh`) в указанном интерпретаторе.
- [X] Поддержка параллельного (асинхронного) выполнения команд.
- [X] Менеджер `ssh` подключений с проверкой доступности хоста.
- [X] Поддержка навигации по каталогам.
- [ ] Доступ к удаленным хостам по паролю или ключу из конфигурации.
- [ ] Обработка команд, которые требуют ввода от пользователя.
- [ ] Имитация пользовательской сессии для хранение переменных.

## Запуск

Вы можете загрузить предварительно скомпилированный исполняемый файл на странице [релизов](https://github.com/Lifailon/ssh-bot/releases) и запустить бота локально (за обработку параметров отвечает пакет [env](/pkg/env/env.go)) или в контейнере Docker, используя образ из [Docker Hub](https://hub.docker.com/r/lifailon/ssh-bot).

> [!NOTE]
> Перед запуском, необходимо создать своего Telegram бота, используя [@BotFather](https://telegram.me/BotFather) и получить его `API Token`, который необходимо указать в конфигурационном файле.

- Создайте рабочий каталог:

```shell
mkdir ssh-bot
cd ssh-bot
```

- Создайте и заполните `.env` файл:

```shell
TELEGRAM_BOT_TOKEN=XXXXXXXXXX:XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
TELEGRAM_USER_ID=7777777777

# Интерпретатор, используемый на локальном хосте (только для Windows)
# Доступные значения: powershell/pwsh
WIN_SHELL=pwsh
# Интерпретатор, используемый на локальном или удаленном хосте (только для Linux) 
# Доступные значения: sh/bash или другой
LINUX_SHELL=bash

# Параллельное (асинхронное) выполнение команд (по умолчанию: false)
PARALLEL_EXEC=true

# Глобальные параметры для ssh подключения (низкий приоритет)
SSH_USER=root
SSH_PORT=22
SSH_CONNECT_TIMEOUT=2

# Список хостов, разделенных запятой (имя пользователя и порт имеют повышенный приоритет)
SSH_HOST_LIST=192.168.3.102,192.168.3.103,lifailon@192.168.3.105:2121
```

> [!NOTE]
> Доступ к боту ограничен идентификатором пользователя. Вы можете узнать Telegram `id`, используя [@getmyid_bot](https://t.me/getmyid_bot) или в логах бота, при отправки ему сообщения.

- Запустите бота в контейнере:

```shell
docker run -d --name ssh-bot \
    -v ./.env:/ssh-bot/.env \
    -v $HOME/.ssh/id_rsa:/root/.ssh/id_rsa \
    --restart unless-stopped \
    lifailon/ssh-bot:latest
```

> [!NOTE]
> Для доступа к удаленным хостам используется авторизация по ключу (в примере, `.ssh/id_rsa`), который необходимо пробросить в контейнер с хостовой системы.

## Сборка

```shell
git clone https://github.com/Lifailon/ssh-bot
cd ssh-bot
docker-compose up -d --build
```